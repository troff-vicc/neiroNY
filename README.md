# Нейро Новый год

**Сервис генерации персонализированных новогодних поздравлений с мультимедиа**

![Главный экран](docs/assets/main-screen.png)

## 🎄 О проекте

Нейро Новый год — это интеллектуальный сервис для создания уникальных новогодних поздравлений. Генерация текстов, фото и видео-открыток с использованием искусственного интеллекта.

**Основные возможности:**
- ✍️ **Генерация текста** — персонализированные поздравления на основе введенных данных
- 🖼️ **Генерация фото** — создание новогодних открыток с AI-изображениями
- 🎬 **Генерация видео** — анимированные поздравления с голосовым сопровождением
- 🎨 **Кастомизация** — выбор стилей, шаблонов и настроек
- 📤 **Экспорт** — скачивание в различных форматах

## 🛠 Технологии

### Frontend
- **React** + **TypeScript** — современный интерфейс
- **Vite** — быстрая сборка
- **Tailwind CSS** — стилизация компонентов
- **Framer Motion** — анимации
- **React Router** — навигация

### Backend
- **FastAPI** — высокопроизводительный Python фреймворк
- **OpenAI API** / **YandexGPT** — генерация текстов
- **Stable Diffusion** / **DALL-E** — генерация изображений
- **FFmpeg** — обработка видео
- **PostgreSQL** — хранение шаблонов и истории

### AI Модули
- **Текст**: GPT-4, Claude, локальные модели
- **Изображения**: Stable Diffusion XL, Midjourney API
- **Видео**: Sora-подобные модели, анимация изображений
- **Озвучка**: Yandex SpeechKit, ElevenLabs

## 🚀 Быстрый старт

### Предварительные требования
- Python 3.10+
- Node.js 18+
- FFmpeg (для видео модуля)
- API ключи для выбранных AI-сервисов

### Установка и запуск

1. **Клонирование репозитория**
```bash
git clone https://github.com/your-username/newyear-generator
cd newyear-generator
```

2. **Настройка бэкенда**
```bash
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt

# Настройка переменных окружения
cp .env.example .env
# Отредактируйте .env, добавив API ключи

python manage.py migrate
python manage.py runserver
```
Бэкенд будет доступен по адресу: `http://localhost:8000`

3. **Настройка фронтенда**
```bash
cd frontend
npm install
npm run dev
```
Фронтенд будет доступен по адресу: `http://localhost:5173`

4. **Запуск AI воркеров (опционально)**
```bash
cd workers
python text_worker.py  # Для генерации текста
python image_worker.py  # Для генерации изображений
python video_worker.py  # Для генерации видео
```

## 🎯 Модули генерации

### Текст-генератор (`/text-generator`)
```javascript
// Пример запроса
{
  "recipient_name": "Анна",
  "relationship": "коллега",
  "tone": "официальный",
  "length": "средний",
  "additional_info": "успешный проект в 2024"
}
```

### Фото-генератор (`/image-generator`)
- Выбор стиля (реализм, мультяшный, винтаж и т.д.)
- Загрузка собственных фото
- Добавление текста на изображение
- Редактирование элементов

### Видео-генератор (`/video-generator`)
- Анимация текста и изображений
- Фоновые видео с праздничной тематикой
- Голосовое озвучивание
- Музыкальное сопровождение

## 📸 Скриншоты

![text-generator.png](docs%2Fassets%2Ftext-generator.png)

![image-generator.png](docs%2Fassets%2Fimage-generator.png)

![video-generator.png](docs%2Fassets%2Fvideo-generator.png)

![templates.png](docs%2Fassets%2Ftemplates.png)

## 📁 Структура проекта

```
newyear-generator/
├── backend/
│   ├── app/
│   │   ├── api/
│   │   │   ├── text_generator/     # API генерации текста
│   │   │   ├── image_generator/    # API генерации изображений
│   │   │   ├── video_generator/    # API генерации видео
│   │   │   └── templates/          # Управление шаблонами
│   │   ├── core/                   # Основная логика
│   │   ├── models/                 # Модели данных
│   │   └── services/               # Бизнес-логика
│   ├── workers/                    # Фоновые задачи
│   └── requirements.txt
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   │   ├── TextGenerator/      # Компонент генерации текста
│   │   │   ├── ImageGenerator/     # Компонент генерации фото
│   │   │   ├── VideoGenerator/     # Компонент генерации видео
│   │   │   └── Editor/             # Редактор контента
│   │   ├── pages/                  # Страницы приложения
│   │   ├── hooks/                  # Кастомные хуки
│   │   ├── utils/                  # Вспомогательные функции
│   │   └── types/                  # TypeScript типы
│   ├── public/
│   └── package.json
├── docs/                           # Документация
│   └── assets/                     # Медиафайлы
├── docker-compose.yml              # Docker конфигурация
└── README.md
```

## 🐳 Docker развертывание

```bash
# Запуск полного стека
docker-compose up -d

# Только бэкенд с PostgreSQL
docker-compose up backend db

# Только фронтенд
docker-compose up frontend
```

## 🔧 Конфигурация

Создайте `.env` файл в корне backend:

```env
OPENAI_API_KEY=your_key_here
STABILITY_API_KEY=your_key_here
ELEVENLABS_API_KEY=your_key_here
DATABASE_URL=postgresql://user:pass@localhost/newyear_db
REDIS_URL=redis://localhost:6379
```

## 👥 Команда

- **Full-Stack разработчик** — архитектура, бэкенд, AI интеграция
- **Frontend разработчик** — интерфейсы, анимации
- **AI инженер** — настройка моделей, оптимизация
- **Дизайнер** — UI/UX, шаблоны, брендинг
- **Тестировщик** — QA, пользовательские сценарии

## 📄 Лицензия

MIT License. Проект создан для новогоднего хакатона.

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для фичи (`git checkout -b feature/amazing`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте ветку (`git push origin feature/amazing`)
5. Откройте Pull Request

---

**Нейро Новый год — Ваши поздравления будут незабываемыми!** 🎅🎁✨